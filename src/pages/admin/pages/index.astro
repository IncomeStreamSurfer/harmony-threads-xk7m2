---
export const prerender = false;
import AdminLayout from "../../../layouts/AdminLayout.astro";
import { ssrClient } from "../../../lib/supabase";

const sb = ssrClient(Astro.cookies);
const { data: { user } } = await sb.auth.getUser();
if (!user) return Astro.redirect("/login?next=/admin/pages");
const { data: admin } = await sb.from("admins").select("email").eq("email", user.email ?? "").maybeSingle();
if (!admin) return new Response("Forbidden", { status: 403 });

const { data: pages } = await sb.from("pages").select("id, slug, title, published_at").order("updated_at", { ascending: false });
---
<AdminLayout title="Pages">
  <div class="bg-white rounded-lg border overflow-hidden" style="border-color: #e5e7eb;">
    <table class="w-full text-sm">
      <thead style="background: #f9fafb;"><tr><th class="text-left px-4 py-3 font-medium">Title</th><th class="text-left px-4 py-3 font-medium">Slug</th><th class="text-center px-4 py-3 font-medium">Status</th><th class="px-4 py-3"></th></tr></thead>
      <tbody>
        {(pages || []).map((p: any) => (
          <tr class="border-t" style="border-color: #f3f4f6;">
            <td class="px-4 py-3 font-medium">{p.title}</td>
            <td class="px-4 py-3 font-mono text-xs" style="color: #6b7280;">/{p.slug}</td>
            <td class="px-4 py-3 text-center"><span class={`text-xs px-2 py-0.5 rounded-full ${p.published_at ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'}`}>{p.published_at ? 'Live' : 'Draft'}</span></td>
            <td class="px-4 py-3 text-right"><a href={`/admin/pages/${p.id}`} class="text-xs font-medium underline" style="color: #c2410c;">Edit</a></td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</AdminLayout>
